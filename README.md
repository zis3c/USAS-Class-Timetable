# USAS Class Timetable

## Security Checks

- `npm run lint:security` runs the security-focused ESLint config.
- CodeQL is set up for manual and weekly runs only.
- GitHub code scanning is not enabled on this repo yet, so CodeQL upload will stay blocked until that setting is available.
